version: 2
jobs:
  build:
    machine: true
    #docker:
    #  - image: circleci/openjdk:8-jdk

    working_directory: ~/repo

    environment:
      #_JAVA_OPTIONS: "-XX:+UnlockExperimentalVMOptions -XX:+UseCGroupMemoryLimitForHeap"
      GRADLE_OPTS: -Xmx256m -Dorg.gradle.daemon=true -Dorg.gradle.caching=true
      TERM: dumb
    
    steps:
      - checkout
      - restore_cache:
          keys:
            - gradle-aws-plugin-{{ .Branch }}-{{ checksum ".circleci/config.yml" }}-{{ checksum "build.gradle" }}-{{ checksum "gradle/wrapper/gradle-wrapper.properties" }}

      - run:
          name: Java version
          command: java -version
      - run:
          name: Build Core
          command: ./gradlew core:reportScoverage
      - run:
          name: Build everything else
          command: ./gradlew utils:reportScoverage features:reportScoverage readers:reportScoverage testkit:reportScoverage cli:reportScoverage -x core:reportScoverage
      - run:
          name: Collect test results
          command: mkdir -p ~/test-results/ && find . -type f -regex ".*/build/test-results/testScoverage/.*xml" -exec cp {} ~/test-results/ \;
      
      - store_test_results:
          path: ~/test-results

      - save_cache:
          key: gradle-aws-plugin-{{ .Branch }}-{{ checksum ".circleci/config.yml" }}-{{ checksum "build.gradle" }}-{{ checksum "gradle/wrapper/gradle-wrapper.properties" }}
          paths:
            - ~/.gradle
            - ~/.m2

      - run:
          name: Report scoverage resuts to Codecov
          command: bash <(curl -s https://codecov.io/bash)

workflows:
  version: 2
  build:
    jobs:
      - build
